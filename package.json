{
  "name": "codebase-doctor",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Assisted Dependency Upgrade Doctor powered by IBM Bob 2.0",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=backend",
    "test": "npm run test --workspace=backend",
    "lint": "npm run lint --workspace=backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
